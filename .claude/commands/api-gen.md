根据以下描述，为本项目生成一个新的 API 模块文件。

项目 API 规范：
- 文件位置：`my_app/src/api/`
- 使用 axios 封装，参考现有 `auth.ts` 风格
- 包含完整的 TypeScript 类型定义
- 错误处理统一走拦截器

需求描述：$ARGUMENTS

请生成：
1. API 函数文件（`my_app/src/api/xxx.ts`）
2. 对应的类型定义（`my_app/src/types/xxx.ts`）
3. 简要说明每个接口的用途
